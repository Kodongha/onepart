<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="complaint">
	<select id="menuComplaint"
		resultType="com.kh.complaint.ComplaintVo">
		SELECT COMPLAINT_SEQ, COMPLAINT_TITLE, RESIDENT_SEQ,
		COMPLAINT_CONTENT, COMPLAINT_ENROLL_DT, COMPLAINT_STATUS
		FROM COMPLAINT
		ORDER BY COMPLAINT_SEQ
	</select>
</mapper>